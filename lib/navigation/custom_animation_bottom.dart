import 'package:flutter/material.dart';

class CustomBottomAnimation extends StatelessWidget {
  const CustomBottomAnimation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
